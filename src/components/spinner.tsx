const Spinner = () => {
  return (
    <span role="status" aria-label="Loading..." className="animate-spin rounded-full h-8 w-8 border-t-3 border-blue-600 inline-block"></span>
  );
};

export default Spinner;
